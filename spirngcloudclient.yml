spring:
  profiles:
    active:
    - dev
---
service:
  prot: 8021
spring:
  profile: dev
  application:
  name: configclient
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
---
service:
  prot: 8022
spring:
  profile: test
  application:
    name: configclient
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
